# JECO+ Backend - Production Environment
# This file is a template. Actual values are stored in Google Secret Manager.

NODE_ENV=production
PORT=3000

# Database (Cloud SQL - High Availability)
DB_HOST=/cloudsql/jecoplus-production:asia-southeast1:jecoplus-db
DB_PORT=5432
DB_NAME=jecoplus
DB_USER=jecoplus
DB_PASSWORD=<from-secret-manager>
DB_SSL=true
DB_POOL_MIN=2
DB_POOL_MAX=10

# Redis (Cloud Memorystore - High Availability)
REDIS_HOST=10.x.x.x
REDIS_PORT=6379

# JWT Configuration (Strong secrets required)
JWT_ACCESS_SECRET=<from-secret-manager>
JWT_REFRESH_SECRET=<from-secret-manager>
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Firebase Admin SDK
FIREBASE_PROJECT_ID=jecoplus-production
# Service account credentials loaded via GOOGLE_APPLICATION_CREDENTIALS

# CORS (Production domains only)
CORS_ORIGINS=https://jecoplus.web.app,https://jecoplus.firebaseapp.com,https://www.jecoplus.com

# Rate Limiting (Stricter for production)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=60

# Logging
LOG_LEVEL=info
